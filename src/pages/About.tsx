export default function About() {
  return (
    <section>
      <h1>About</h1>
      <p>
        This is a small Vite + React + TypeScript demo. It is used to demo a
        supply-chain security tool that scans npm dependencies on pull requests.
      </p>
    </section>
  );
}
